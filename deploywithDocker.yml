---
- hosts: websever
  become: yes
  vars:
    build_number: "{{ build_number }}"
  tasks:
      
    - name: login to jfrog
      shell: docker login -u test -p Test@12345 trialtypobk.jfrog.io  
    
    - name: pull an image
      shell: docker pull trialtypobk.jfrog.io/dockerbhargav/sampledockeruplaod:v1.0-${build_number}

    - name: remove container
      shell: docker rm -f devopsAppDeploy
        
    - name: create a application container
      shell: docker run -id --name devopsAppDeploy -p 8111:8080 trialtypobk.jfrog.io/dockerbhargav/sampledockeruplaod:v1.0-${build_number}
 
   
